extends CanvasLayer

func show_win_screen(flag: bool): #this flag can only be a boolean
	$WinScreen.visible = flag #clicked eye in inspector/scene for it be off by default. this line turns can it turn it on or off
